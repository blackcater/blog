{
  "private": true,
  "engines": {
    "yarn": "<2.0.0",
    "node": ">=12.0.0"
  },
  "scripts": {
    "clean": "gatsby clean",
    "start": "npm run clean && gatsby develop",
    "build": "npm run clean && gatsby build"
  },
  "dependencies": {
    "@unisite/theme-blog": "next",
    "gatsby": "3.4.0",
    "gatsby-plugin-compile-es6-packages": "2.1.0",
    "react": "17.0.2",
    "react-dom": "17.0.2"
  },
  "devDependencies": {
    "prettier": "2.2.1"
  }
}
